[package]
name = "koma_manhuadui_source"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
koma_source_sdk = { path = "../../sdk" }

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
strip = true
